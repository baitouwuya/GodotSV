# 编辑器集成与导入

## 打开 GDSV 文件

- 在文件系统面板双击 `.gdsv` 文件即可打开内置表格编辑器。

## CSV/TSV 导入

- 将 `.csv` 拖入项目，导入面板可设置表头、分隔符、Schema。

## Schema 管理

1. 新建 `GDSVSchema` 资源。
2. 配置字段类型与验证规则。
3. 在加载器中 `.with_schema(schema)`。

## 表格编辑器能力

- 单元格编辑、撤销/重做
- 搜索与替换
- 实时验证与统计

